import React, { useEffect, useState } from 'react';
import LoginVerifier from '../component/common/LoginVerifier'

const Home = (props) => {
    LoginVerifier(props)
    return (
        <>
        </>
    );
}

export default Home